[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/bin/sh /app/railway-entrypoint.sh"
healthcheckPath = "/healthz"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
